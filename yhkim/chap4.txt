4.0) 
expo 사용해서 Publish 가능
그럼 다른 사람들과 함께 공유도 가능하고 expo 에서 공개됨
웹으로도 가능함. 

rn 코드를 웹용으로 쓸 때, react 로 바꿔줌
ex) view -> div 로 자동 변경

4.1)
웹에서는 alert 이 작동 x platform 사용 

confirm api 를 사용.
icons 도 asset 이라는 곳에서 사용 가능.


4.2)
app.json 에서 뒷배경 등등 바꿀 수 있음
아이콘 바꿀 시 웹과 앱스토에서 볼 수 있음

앱스토에 퍼블리시, 99 달러 안드로이드는 35

expo cli 에서 쉽게 할 수 있음
expo build:android 
를 만들고 다운로드할 수 있음
컴퓨터에서 만드는 것 x expo 서버에서 만듦

rn for windos or ios 
-> mas os, window 용 앱오 만들 수 있음


npm -i gh-pages
script 에 "deploy", plredeploy conf 추가


4.3)
sdk 도 만들 수 있음

기초적인 파일들에는 접근 x 
앱을 만들때 가끔 계약을 위반하는 부분이 될 수 있음
대부분의 app 은 expo 로도 괜찮을 수도..
js 랑 설정 부분만 접근 가능

ex)
블루투스 기기와 연결이 가능해야하는 어플 -> expo sdk 에 없음
따로 라이브러리 설치해서 여러 파일에 접근해야하는데 expo 내에서는 못함.

어플 사이즈가 좀 큼.. => 모든 것들이 가능하게끔 sdk 를 추가해서 배포하기 때문
앱 크기르ㅡㄹ 줄이고 싶어도 줄일 수 xx

ejext 라는 것을 사용, 다른 파일에 접근, 빼낼 수 있음

create reactive natice 라는 것을 사용.










